package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.eventloop.opmode.TeleOp;
import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;

@TeleOp

public class NewRobotController extends LinearOpMode {
    public void runOpMode() {
        Bendy.setUp(hardwareMap);

        waitForStart();

        double speedAdjust = 0.8;
        while(opModeIsActive()) {
            Bendy.drive(gamepad1.left_stick_y * speedAdjust, gamepad1.left_stick_x * speedAdjust, gamepad1.right_stick_x * speedAdjust);
            if(Bendy.toggleButton("a",gamepad1.a)) Bendy.clamp();
        }
    }
}
